language: generic

sudo: required
services:
    - docker

before_install:
    - docker build -t ghislop/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true ghislop/react-test npm test -- -- coverage

after_success:
    - docker build -t ghislop/multi-client ./client
    - docker build -t ghislop/multi-nginx ./nginx
    - docker build -t ghislop/multi-server ./server
    - docker build -t ghislop/multi-worker ./worker
    # log in to cocker CLI
    - echo "$DOCLER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # take those images and push to docker hub
    - docker push multi-client
    - docker push multi-nginx
    - docker push multi-server
    - docker push multi-worker